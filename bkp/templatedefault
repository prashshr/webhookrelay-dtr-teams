{
	    "type": "AdaptiveCard",
	        "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
		    "version": "1.3",
		        "body": [
				        {
						            "type": "TextBlock",
							                "text": "${push_data.pusher} pushed new tag ${repository.repo_name}:${push_data.tag}" ,
									            "wrap": true
										            }
											        ]
}
